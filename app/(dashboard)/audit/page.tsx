import { AuditLogView } from '@/components/features/audit/AuditLogView';

export const metadata = {
  title: 'ประวัติการใช้งาน | UcomPos',
};

export default function AuditPage() {
  return <AuditLogView />;
}
